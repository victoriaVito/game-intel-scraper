### Scraper Rules

- Use Playwright for dynamic/JS-heavy pages, BeautifulSoup for static HTML.
- Throttle, handle timeouts and retries automatically.
- Clean and normalize all outputs.
- Only yield data after store validation if fields match official info.