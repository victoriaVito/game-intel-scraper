# Add Scraper Prompt

Generate a new scraper following the base scraper and project conventions. Name the module `NAME_scraper.py`.
Use Playwright or BeautifulSoup as warranted, and validate all fields as per the pipeline and store validation module.